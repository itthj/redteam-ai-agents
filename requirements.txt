# ─── Core AI ───────────────────────────────────────────────────────────────────
anthropic>=0.109.1           # Claude SDK — Opus 4.8, adaptive thinking, effort, caching
mcp>=1.27.2                  # Model Context Protocol — connect agents to external tools
mcp-server-fetch>=2026.6.4  # MCP fetch server — the ready-to-use 'web' server

# ─── Web / API layer ───────────────────────────────────────────────────────────
fastapi>=0.136.3
uvicorn[standard]>=0.49.0
python-multipart>=0.0.32

# ─── Validation & config ───────────────────────────────────────────────────────
pydantic>=2.13.4
pydantic-settings>=2.14.1
python-dotenv>=1.2.2

# ─── Async / HTTP ──────────────────────────────────────────────────────────────
aiohttp>=3.14.1
aiofiles>=25.1.0
httpx>=0.28.1

# ─── Storage ───────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.50
aiosqlite>=0.22.1           # async SQLite for evidence store

# ─── Attack graph (2A) ──────────────────────────────────────────────────────────
networkx>=3.6.1               # in-process attack graph — the default backend
# neo4j>=5.0                 # OPTIONAL — live BloodHound-style graph browser (see .env)

# ─── Tracing (5C) ───────────────────────────────────────────────────────────────
opentelemetry-api>=1.42.1
opentelemetry-sdk>=1.42.1
# opentelemetry-exporter-otlp-proto-http>=1.20  # OPTIONAL — needed to actually export spans

# ─── Semantic memory (2C, optional) ──────────────────────────────────────────────
# sentence-transformers>=3.0   # OPTIONAL — embedding-based tradecraft recall;
#                                degrades to keyword overlap when absent (MEMORY_SEMANTIC_RECALL)

# ─── Security / crypto ─────────────────────────────────────────────────────────
cryptography>=48.0.1
bcrypt>=5.0.0

# ─── Networking / recon tools ──────────────────────────────────────────────────
python-nmap>=0.7.1          # Nmap wrapper (nmap must be installed on host)
dnspython>=2.8.0            # DNS enumeration
shodan>=1.31.0              # Shodan API
requests>=2.34.2

# ─── CLI / output ──────────────────────────────────────────────────────────────
click>=8.4.1
rich>=15.0.0                # beautiful terminal output
tabulate>=0.10.0

# ─── Templating (reports) ──────────────────────────────────────────────────────
jinja2>=3.1.6

# ─── Testing ───────────────────────────────────────────────────────────────────
pytest>=9.0.3
pytest-asyncio>=1.4.0
